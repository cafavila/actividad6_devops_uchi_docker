version: 2.1
jobs:
  build:
    docker:
      - image: cimg/node:17.8.0
    steps:
      - checkout
      - run:
          name: "Paso 1" 
          command: | 
            echo "Se construye el contenedor"
            node --version
workflows:
  build_and_test:
    jobs:
      - build
